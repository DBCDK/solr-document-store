{
    "appName": "solr-docstore-service",
    "jdbc": {
        "jdbc/solr-doc-store": {
            "xa": false,
            "url": "${DOCSTORE_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}"
        }
    }
}
