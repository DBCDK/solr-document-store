{
    "appName": "solr-docstore-monitor",
    "jdbc": {
        "jdbc/solr-doc-store": {
            "xa": false,
            "url": "${DOCSTORE_POSTGRES_URL}",
            "maxSize": "${POOL_SIZE}",
            "steadySize": 0
        }
    }
}
